# 面向 Agent 长期任务的自生长文档体系 · 总纲领

> **本文件是本体系的入口与原则说明。**  
> **过程权威不在本文，而在 [`workflow.md`](./workflow.md)。**  
> Agent 在开始任何实质工作之前，应先读本文以建立心智模型，再严格遵循 `workflow.md`。

---

## 1. 我们要解决什么

长期任务中，Agent 失败往往不是「不会写代码」，而是：

- **边界丢失**：权限、环境、可做/不可做在跨会话后被遗忘；
- **过程漂移**：本应回归测试、本应更新状态，却在多轮后悄悄跳过；
- **状态腐烂**：已知 bug、已否决路径、结构认知只活在对话里，下一会话重头踩坑；
- **体系僵死**：文档一次写完不再进化，或反过来无序膨胀，比任务本身更重。

本体系把 Agent 的**外部记忆**与**操作规程**显式化，并要求它们随任务**自生长、可修剪**。

定位一句话：

> 这不是给人看的普通 README，而是 **Agent 的外部海马体 + 运行时规程**。

---

## 2. 四层结构

任何采用本体系的工作，由以下四层构成。  
其中 **a / b / c 为固定角色**；**d 为开放集合**，具体成员由任务与 Agent 共同决定。

### a. 总纲领（本文件）

| 项目 | 说明 |
|------|------|
| 职责 | 阐明体系思想、层级边界、权威顺序、文档索引入口 |
| 不负责 | 逐步操作细则（那些属于 workflow） |
| 更新时机 | 体系原则变更、工作目标/成功标准变更、顶层索引需要调整时 |

### b. Workflow（[`workflow.md`](./workflow.md)）

| 项目 | 说明 |
|------|------|
| 职责 | **整个 Agent 工作必须遵循的过程法** |
| 地位 | **过程唯一权威** |
| 自生长 | 每轮工作后必须审视：是否所有文档的读写/更新时机都已在 workflow 中描述 |

### c. Compact Summary（[`compact-summary.md`](./compact-summary.md)）

| 项目 | 说明 |
|------|------|
| 职责 | 会话将满/中断/交接时的续跑缓存 |
| 性质 | 会话级，不是项目知识库；长期决策应提升到 d |

### d. 被归类的文档（开放集）

| 文档 | 说明 |
|------|------|
| [`d/constraints.md`](./d/constraints.md) | 硬边界：只读会话数据、不改用户 agent 历史 |
| [`d/codemap.md`](./d/codemap.md) | 代码结构现状 |
| [`d/session-stores.md`](./d/session-stores.md) | 各 Agent 会话落盘布局与 resume 入口 |
| [`d/尚存bug.md`](./d/尚存bug.md) | 跟踪中的缺陷 |
| [`d/应该被忽略的bug.md`](./d/应该被忽略的bug.md) | 已知不修/误报 |

---

## 3. 权威层级

1. **约束类 d** — 能不能做  
2. **`workflow.md`** — 怎么做、做到什么算完成  
3. **其他 d** — 现状是什么（真实磁盘/环境是最终事实）  
4. **Compact Summary** — 刚才做到哪  
5. **总纲领（本文）** — 为何如此组织  

---

## 4. 自生长原则

1. **结晶**：compact 存不住的反复信息 → 写入 d  
2. **挂靠**：新 d 必须在 workflow 写清读写时机  
3. **修剪**：合并同质、归档过期、删除无职能  
4. **workflow 自指**：更新时机是否都在 workflow 里  

---

## 5. 读取与续跑（心智默认）

```text
会话开始
  → 读总纲领 → 读 workflow.md → 按需加载 d → 若有 compact 且未完成则恢复

工作过程中
  → 严格按 workflow 阶段、门禁、禁止事项

上下文将满 / 会话将结束
  → 更新 compact-summary.md；重要决策提升到 d

每一轮工作后
  → 状态同步 + 元进化检查
```

---

## 6. 敏感信息

可描述：会话根路径、环境变量名、CLI 用法。  
**禁止**写入：明文密码、私钥、长期高权限 token、完整 transcript 正文到常驻文档。

---

## 7. 本工作的目标与索引

### 7.1 当前工作是什么

- **目标：** 构建 `agent_session_history`——统一列举本机各类 Agent 会话（Grok Build、Qoder 等），并回答「去哪续跑、复制什么命令」。
- **成功标准：**
  1. 跨 cwd 全集发现 Grok + Qoder 历史会话（非仅当前目录）；
  2. 每条可展示：来源、标题、上次活动（紧凑相对时间）、消息条数、**续跑目录原文**、健康态；
  3. TUI 可交互浏览（差分绘制、宽屏分栏）；详情给出路径绑定说明与 resume 命令；
  4. Qoder 路径硬绑定 / Grok ID 可跨目录 的语义区分正确；
  5. `--list` / `--json` 可用；文档体系挂靠 workflow。
- **非目标 / 不做的事（当前阶段）：**
  - 不自动 spawn 交互 resume 子进程（`r` 仅展示命令）；
  - 不修改/删除用户 agent 会话文件；
  - 不上传会话内容；
  - 不复刻完整 transcript 阅读器。

### 7.2 文档索引

| 文档 | 角色 | 说明 |
|------|------|------|
| [总纲领.md](./总纲领.md) | a | 本文件 |
| [workflow.md](./workflow.md) | b | 过程权威 |
| [compact-summary.md](./compact-summary.md) | c | 续跑摘要 |
| [README.md](./README.md) | 人读 | 用法与界面导读 |
| [d/constraints.md](./d/constraints.md) | d | 硬边界 |
| [d/session-stores.md](./d/session-stores.md) | d | 落盘布局与 resume 语义 |
| [d/ui-tui.md](./d/ui-tui.md) | d | TUI 布局/列/快捷键 |
| [d/codemap.md](./d/codemap.md) | d | 代码结构 |
| [d/尚存bug.md](./d/尚存bug.md) | d | 缺陷跟踪 |
| [d/应该被忽略的bug.md](./d/应该被忽略的bug.md) | d | 忽略项 |

---

## 8. 一句话纪律

> **先边界，后过程，再动手；状态写回体系，体系挂靠 workflow；上下文将满先压缩，重要决策要结晶。**
